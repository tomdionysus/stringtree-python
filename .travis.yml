language: python
python:
  - "2.7"
script: 
  - PYTHONPATH=./ coverage run --source=stringtree py.test
  - PYTHONPATH=./  py.test -v --cov coveralls --cov-report term-missing
after_success:
  coveralls